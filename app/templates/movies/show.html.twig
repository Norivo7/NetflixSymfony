{% extends 'movies/index.html.twig' %}

{% block title %}<title>{{ movie.title }} - Netflix</title>{% endblock %}

{% block navigation %}
{#    <div class="navbar">#}
{#        <div class="brandLogoWrapper">#}
{#            <a class="logo" href="{{ path('browse') }}" target="_parent">#}
{#                <img class="logo" src="{{ asset('images/netflixLogoSmall.svg.png') }}" alt="av1"/>#}
{#            </a>#}
{#        </div>#}
{#    </div>#}
{% endblock %}

{% block body %}
{#    <body>#}
    <div class="movie_container">
        <div class="text-center">
            <div>
                <iframe id="asd" style="border: 0;" width="100%" height="100%" src="{{ movie.link }}?autoplay=1"
                        allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
                        allowfullscreen>
                </iframe>
                <a href="{{ path('browse') }}" target="_parent">
                <img class="logo" src="{{ asset('images/netflixLogoSmall.svg.png') }}" alt="av1"/>
                </a>
{#                <h3>{{ movie.title }}</h3>#}
{#                <p>Opis : <br>{{ movie.description }}</p>#}
{#                <p>Rok : {{ movie.year }} <br>#}
{#                <p>Kategorie :</p>#}
{#                {% for category in categories %}#}
{#                    <li> {{ category.name }}</li>#}
{#                {% endfor %}#}
            </div>

        </div>
    </div>
{#    </body>#}
{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <style>

        {#======================
                 Global
        ======================#}

        html {
            font-family: 'Netflix Sans Regular', "Helvetica Neue", "Helvetica", Arial, sans-serif;
            font-weight: normal;
        }
        iframe {
            position: relative;
            /*bottom: 50px;*/
            /*z-index: -1;*/
        }
        {#======================
                 Buttons
        ======================#}

        .my-list-button {
            color: gray;
            padding: .5em 1.5em;
            letter-spacing: 2px;
            cursor: pointer;
            background-color: transparent;
            border: 1px solid grey;
            text-decoration: none;
        }

        {#======================
               Navigation
        ======================#}

        .navbar a:hover {
            opacity: 0.8;
        }

        .navbar {
            overflow: hidden;
            width: 100%;
        }

        .brandLogoWrapper {
            margin-left: 30px;
            margin-top: 18px;
        }

        .logo {
            position: absolute;
            height: 31px;
            right: 1760px;
            top: 30px;
            z-index: 2;
        }
        .movie_container {
             margin-left: 0px;
        }
    </style>

{% endblock %}
{% block javascript %}


{% endblock %}
