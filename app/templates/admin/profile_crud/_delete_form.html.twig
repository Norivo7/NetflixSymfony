<form method="post" action="{{ path('profile_crud_delete', {'id': profile.id}) }}" onsubmit="return confirm('Czy na pewno chcesz usunąć ten rekord?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profile.id) }}">
    <button class="deleteButton">Usuń</button>
</form>
